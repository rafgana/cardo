interface:
  display_name: "CodeMap"
  short_description: "Generate, update, and drift-check agent-facing code terrain maps."
  default_prompt: "Use the codemap skill to create, update, or drift-check a task-scoped CodeMap that routes agent context progressively from project or feature scope down to source-linked evidence."
